<?xml version="1.0" encoding="UTF-8" ?>
<odoo>

    <record id="contact_relation_view_tree" model="ir.ui.view">
        <field name="name">contact.relation.view.list</field>
        <field name="model">contact.relation</field>
        <field name="arch" type="xml">
            <list string="Contact Relation" editable="bottom">
                <field name="question_id"/>
                <field name="contact_id"/>
            </list>
        </field>
    </record>

</odoo>
